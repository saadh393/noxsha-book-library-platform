# নোকশা প্ল্যাটফর্ম - সেটআপ ও ব্যবহার নির্দেশিকা

## প্রকল্প পরিচিতি
নোকশা একটি আধুনিক ডিজিটাল ই-বুক লাইব্রেরি প্ল্যাটফর্ম, যেখানে Next.js 15 (App Router), TypeScript, Tailwind CSS এবং Framer Motion ব্যবহার করা হয়েছে। এতে রয়েছে চমৎকার জনসাধারণের অভিজ্ঞতা এবং সমৃদ্ধ অ্যাডমিন ড্যাশবোর্ড।

## দ্রুত শুরু

### ইনস্টলেশন
```bash
npm install
npm run dev
```

অ্যাপটি চলবে `http://localhost:3000` ঠিকানায়।

### প্রোডাকশন বিল্ড
```bash
npm run build
npm run start
```

---

## অ্যাডমিন অ্যাক্সেস

### অ্যাডমিন লগইন
হেডারের ডান দিকে থাকা 🔒 আইকনে ক্লিক করুন। এরপর অ্যাডমিন লগইন পাতায় নিয়ে যাওয়া হবে।

### ডেমো ক্রিডেনশিয়াল
ডিফল্টভাবে ডাটাবেজ স্ক্রিপ্টে একটি অ্যাডমিন ব্যবহারকারী যুক্ত আছে:

- ইমেল: `admin@noxsha.com`  
- পাসওয়ার্ড: `Admin@123`  

> **গুরুত্বপূর্ণ:** সরাসরি প্রোডাকশনে ব্যবহারের আগে অবশ্যই নতুন পাসওয়ার্ড সেট করুন অথবা নতুন অ্যাডমিন যোগ করুন।

---

## বৈশিষ্ট্যসমূহ

### জনসাধারণের ওয়েবসাইট
- **হোম পাতা**: হিরো, নির্বাচিত বই, বিভাগসমূহ
- **অনুসন্ধান**: শিরোনাম বা লেখক দিয়ে লাইভ সার্চ
- **বইয়ের বিবরণ**: সম্পূর্ণ তথ্য, রিভিউ ও ডাউনলোড অপশন
- **ডাউনলোড মডাল**: বিনামূল্যে ডাউনলোডের জন্য ফর্ম
- **রেসপন্সিভ ডিজাইন**: সব ডিভাইসে কাজ করে

### অ্যাডমিন ড্যাশবোর্ড
1. **বই ব্যবস্থাপনা**: নতুন ই-বুক যুক্ত ও তালিকা পর্যালোচনা  
2. **হিরো সেকশন**: হোম পৃষ্ঠার শিরোনাম ও উপশিরোনাম সম্পাদনা  
3. **রিভিউ**: ব্যবহারকারীর রিভিউ অনুমোদন/মোছা  
4. **সোশ্যাল লিংক**: ফুটারে প্রদর্শিত লিংক নিয়ন্ত্রণ  
5. **সাইট সেটিংস**: সামগ্রিক কনফিগারেশন পরিচালনা  

---

## রঙের প্যালেট

- **প্রধান**: `#884be3`  
- **গৌণ**: `#6B4BA8`  
- **গাঢ়**: `#2D1B4E`  
- **উজ্জ্বল**: `#FAF7FF`  

---

## ডেটাবেস

- প্রযুক্তি: **MySQL 8** (অথবা সামঞ্জস্যপূর্ণ সংস্করণ)  
- ডিফল্ট কনফিগারেশন `.env.example`-এ দেওয়া আছে  
- সম্পূর্ণ স্কিমা ও সিড ডেটা `database/schema.sql` ফাইলে অন্তর্ভুক্ত  

### ডাটাবেজ তৈরি ও সিড করা

```bash
mysql -u root -p < database/schema.sql
```

> প্রয়োজনে `mysql` কমান্ডে `-h` বা `-P` দিয়ে হোস্ট এবং পোর্ট নির্দিষ্ট করুন। স্ক্রিপ্টটি `noxsha` নামে ডাটাবেজ তৈরি করবে এবং প্রয়োজনীয় টেবিল, নমুনা বই, সোশ্যাল লিংক ও ডিফল্ট অ্যাডমিন ব্যবহারকারী যোগ করবে।

বিদ্যমান ডাটাবেজ আপডেট করার সময় `database/migrations/` ডিরেক্টরির স্ক্রিপ্টগুলো চালিয়ে নিন। উদাহরণ:

```bash
mysql -u root -p noxsha < database/migrations/20251102_add_storage_columns.sql
```

### টেবিলসমূহ

- `books`: ই-বুক সংকলন (শিরোনাম, লেখক, বিভাগ, মূল্য প্রভৃতি)  
- `site_settings`: হিরো শিরোনাম, উপশিরোনাম ও ফুটার বিবরণ  
- `reviews`: ব্যবহারকারীর রিভিউ, অনুমোদন অবস্থা  
- `social_links`: ফুটারে প্রদর্শিত লিংকসমূহ  
- `downloads`: ই-বুক ডাউনলোড ফর্ম সাবমিশন  
- `admin_users`: অ্যাডমিন লগইন তথ্য (পাসওয়ার্ড `bcrypt` হ্যাশ আকারে সংরক্ষিত)

### অ্যাক্সেস নিয়ন্ত্রণ

- অ্যাডমিন API গুলোতে HttpOnly কুকি-ভিত্তিক সেশন যাচাই করা হয়  
- জনসাধারণের API শুধুমাত্র পাঠযোগ্য ডেটা সরবরাহ করে  
- নতুন অ্যাডমিন যুক্ত করতে `admin_users` টেবিলে হ্যাশড পাসওয়ার্ডসহ রেকর্ড যোগ করুন  

---

## ডিরেক্টরি কাঠামো

```
app/
├── layout.tsx                  # মূল লেআউট ও মেটাডাটা
├── page.tsx                    # জনসাধারণের হোম পাতা
├── globals.css                 # Tailwind ভিত্তিক গ্লোবাল স্টাইল
├── books/
│   └── [id]/page.tsx           # নির্দিষ্ট বইয়ের পাতা
└── admin/
    ├── login/page.tsx          # অ্যাডমিন লগইন
    └── dashboard/page.tsx      # অ্যাডমিন ড্যাশবোর্ড শেল

components/
├── Header.tsx                  # ন্যাভিগেশন
├── Footer.tsx                  # ফুটার ও সোশ্যাল লিংক
├── Hero.tsx                    # হিরো সেকশন
├── Services.tsx                # হাইলাইট বৈশিষ্ট্য
├── CategorySection.tsx         # বিভাগ উপস্থাপন
├── BookSection.tsx             # বই তালিকা
├── BookCard.tsx                # একক বই কার্ড
├── DownloadModal.tsx           # ডাউনলোড ফর্ম
├── home/HomeContent.tsx        # হোম পৃষ্ঠার ডেটা ও লেআউট
├── books/BookDetailsContent.tsx # বইয়ের বিস্তারিত ও সম্পর্কিত বই
└── admin/
    ├── AdminSidebar.tsx
    ├── AdminBooksManager.tsx
    ├── AdminSettings.tsx
    ├── AdminReviews.tsx
    ├── AdminSocialLinks.tsx
    ├── AdminLoginForm.tsx
    └── AdminDashboardShell.tsx

lib/
├── api.ts                      # ফ্রন্টএন্ড থেকে ব্যবহৃত REST ক্লায়েন্ট
├── auth.ts                     # অ্যাডমিন অথ হেল্পার (ফ্রন্টএন্ড)
├── auth-server.ts              # কুকি ও JWT ব্যবস্থাপনা
├── db.ts                       # MySQL কানেকশন পুল
├── serializers.ts              # ডাটাবেজ সারি থেকে টাইপ কনভার্টর
└── types.ts                    # শেয়ারড টাইপ ডেফিনিশন
```

---

## প্রধান প্রযুক্তি

- **Next.js 15 (App Router)**
- **React 18**
- **TypeScript**
- **Tailwind CSS**
- **Framer Motion**
- **Lucide React**
- **MySQL 8** + `mysql2` ড্রাইভার
- **bcryptjs** ও **JSON Web Token** ভিত্তিক অ্যাডমিন অথ

---

## পরিবেশ ভেরিয়েবল

প্রকল্পের মূল ফোল্ডারে `.env.local` তৈরি করুন (নমুনা `.env.example` ফাইলে রয়েছে):

```
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_DATABASE=noxsha
MYSQL_USER=noxsha-db
MYSQL_PASSWORD=saad
AUTH_SECRET=replace-with-strong-secret
```

> প্রোডাকশনে ডেপলয় করার সময় শক্তিশালী `AUTH_SECRET` ব্যবহার করুন এবং ডাটাবেজ ক্রিডেনশিয়াল আপডেট করুন।

---

## কাস্টমাইজেশন

### রং পরিবর্তন
`tailwind.config.js` ফাইলের `theme.extend.colors` অংশ সম্পাদনা করুন।

### লেখা সম্পাদনা
অ্যাডমিন ড্যাশবোর্ড থেকে হিরো শিরোনাম, ফুটার বিবরণ এবং সোশ্যাল লিংক আপডেট করতে পারেন।

### নতুন ফিচার যুক্ত করা
1. পুনর্ব্যবহারযোগ্য অংশ `components/` ডিরেক্টরিতে যোগ করুন  
2. নতুন রুট `app/` ডিরেক্টরিতে তৈরি করুন  
3. দরকার হলে Header বা নির্দিষ্ট লেআউটে নেভিগেশন আপডেট করুন  

---

## কর্মদক্ষতা

- Next.js এর বিল্ট-ইন কোড স্প্লিটিং ও রুট-ভিত্তিক লোডিং  
- অ্যানিমেশনের জন্য অপ্টিমাইজড Framer Motion  
- MySQL সমর্থিত কাস্টম Next.js API রুটের মাধ্যমে দ্রুত ডেটা ফেচিং  
